# Frontend Coding Rules

이 프로젝트의 HTML, CSS, JavaScript를 작성하거나 수정할 때 아래 규칙을 기본 개발 원칙으로 적용한다.

## 1. 기본 수정 원칙

새로운 코드를 추가하기 전에 반드시 기존 구조와 관련 코드를 먼저 확인한다.

다음 우선순위에 따라 작업한다.

1. 기존 HTML/CSS/JavaScript 수정
2. 기존 컴포넌트 재사용
3. 기존 디자인 토큰 및 CSS 변수 활용
4. 필요한 경우에만 새로운 클래스/함수 추가
5. 기존 구조로 해결하기 어려운 경우에만 구조 변경

새로운 코드를 계속 추가하여 기존 코드를 덮어쓰는 방식보다 기존 코드를 올바르게 수정하고 정리하는 방식을 우선한다.

---

## 2. 수정 범위

요청받은 작업과 직접 관련된 코드만 수정한다.

정상적으로 동작하는 관련 없는 코드는 임의로 리팩터링하거나 변경하지 않는다.

공통 CSS, 디자인 토큰, 공통 컴포넌트 등 여러 페이지에 영향을 줄 수 있는 코드를 수정해야 하는 경우 영향 범위를 먼저 확인한다.

구조 변경이 필요한 경우 기존 구조를 임의로 크게 변경하지 않는다.

큰 구조 변경이 필요하다면 변경하기 전에 다음 내용을 설명한다.

- 변경이 필요한 이유
- 변경되는 파일 또는 컴포넌트
- 기존 코드에 미치는 영향

현재 작업과 관련 없는 기존 코드의 규칙 위반을 발견하더라도 임의로 수정하지 않는다.

필요한 경우 별도로 알려준다.

---

## 3. CSS 디자인 토큰

CSS 값을 작성할 때 `common.css`에 정의된 기존 CSS 변수를 우선적으로 사용한다.

### Font Size

`font-size`는 `--fs-*` 형태의 기존 CSS 변수를 사용한다.

가능한 경우 직접적인 px 값 작성을 피한다.

### Line Height

`line-height`는 `--lh-*` 형태의 기존 CSS 변수를 사용한다.

### Spacing

margin, padding, gap 등 레이아웃 간격은 `--space-*` 형태의 기존 CSS 변수를 우선 사용한다.

사용자가 "간격을 넓혀줘", "간격을 줄여줘" 등의 요청을 하는 경우 임의의 수치를 만들기 전에 `common.css`의 기존 `--space-*` 변수를 확인한다.

### Color

색상은 `common.css`에 정의된 기존 색상 변수를 우선 사용한다.

동일한 색상의 HEX, RGB, HSL 값을 여러 위치에서 직접 작성하지 않는다.

### 새로운 토큰

필요한 값과 정확히 일치하는 변수가 없더라도 새로운 값을 즉시 만들지 않는다.

먼저 기존 디자인 토큰 중 의미상 적절한 값을 사용할 수 있는지 확인한다.

새로운 디자인 토큰이 반드시 필요한 경우 추가 이유를 설명한다.

---

## 4. CSS 금지 사항

다음 방식은 특별한 이유가 없는 한 사용하지 않는다.

- 불필요한 `!important`
- 동일 속성을 반복해서 override하는 방식
- inline style
- `letter-spacing`
- `clamp()`
- `min()`
- `max()`
- 비슷하거나 중복되는 media query
- 의미 없는 클래스 추가
- 특정 화면만 맞추기 위한 임시 CSS
- specificity를 높여 문제를 덮어쓰는 방식
- 지나치게 깊은 CSS selector
- 스타일링 목적의 ID selector
- 불필요한 `nth-child` 의존
- 매직 넘버의 반복 사용

문제를 해결하기 위해 임시 CSS를 계속 누적하지 않는다.

---

## 5. Layout

레이아웃은 다음 순서로 우선 고려한다.

1. CSS Grid
2. Flexbox
3. 일반 document flow
4. 필요한 경우에만 position

`position: absolute`는 일반적인 페이지 레이아웃 구성 목적으로 사용하지 않는다.

Flexbox 또는 Grid로 해결할 수 있다면 해당 방법을 우선 사용한다.

다만 다음과 같이 요소 중첩 자체가 필요한 UI에서는 사용할 수 있다.

- badge
- overlay
- 이미지 위 UI
- 장식 요소
- 구조적으로 겹쳐야 하는 요소

이 경우에도 absolute 사용 범위를 최소화한다.

---

## 6. CSS Selector

기존 프로젝트의 class naming convention을 유지한다.

동일한 역할을 하는 새로운 클래스를 중복 생성하지 않는다.

선택자는 가능한 단순하게 유지한다.

다음과 같이 지나치게 깊은 선택자 구조를 만들지 않는다.

`main .section .container .list .item .content p`

스타일 충돌을 selector specificity 증가로 해결하지 않는다.

필요하다면 기존 CSS 구조 자체를 확인하여 원인을 해결한다.

---

## 7. Responsive

새로운 media query를 만들기 전에 프로젝트에서 사용하고 있는 breakpoint를 먼저 확인한다.

기존 breakpoint를 우선 사용한다.

동일하거나 비슷한 breakpoint를 중복 생성하지 않는다.

특정 기기 또는 특정 해상도 하나만 맞추기 위한 임시 breakpoint를 만들지 않는다.

Desktop / Tablet / Mobile의 기존 레이아웃 정책을 유지한다.

반응형 수정 시 현재 화면만 확인하지 말고 다른 breakpoint에 미치는 영향도 확인한다.

---

## 8. HTML

HTML을 수정할 경우 기존 구조를 우선 유지한다.

불필요한 wrapper와 `div`를 추가하지 않는다.

가능한 경우 의미에 맞는 semantic HTML을 사용한다.

기존 class naming convention을 유지한다.

CSS 문제를 해결하기 위해 의미 없는 HTML 요소를 추가하지 않는다.

접근성을 고려하여 필요한 경우 적절한 요소, label, alt, aria 속성을 사용한다.

---

## 9. JavaScript

새로운 JavaScript를 작성하기 전에 기존 함수와 이벤트 구조를 확인한다.

기존 기능을 재사용할 수 있다면 재사용한다.

### HTML 콘텐츠 생성 금지

JavaScript를 사용하여 페이지의 정적인 HTML 콘텐츠나 마크업 구조를 생성하지 않는다.

페이지에 처음부터 존재해야 하는 콘텐츠와 구조는 HTML 파일에 직접 작성하고,
JavaScript는 기존 HTML 요소의 상태 변경, 사용자 인터랙션, 데이터 처리 등
동적인 기능을 구현하는 용도로만 사용한다.

다음과 같은 방식으로 HTML 콘텐츠를 JavaScript에서 생성하거나 삽입하지 않는다.

- `innerHTML`을 이용한 마크업 생성
- `insertAdjacentHTML()`을 이용한 마크업 삽입
- `document.createElement()`를 이용한 정적 콘텐츠 구성
- JavaScript 문자열 또는 template literal에 HTML 마크업을 작성하여 삽입
- 반복되는 상품, 카드, 메뉴 등의 정적 콘텐츠를 JavaScript 배열에서 생성하여 HTML에 출력

예를 들어 상품 카드, 메뉴, 배너, 섹션 등 페이지의 기본 콘텐츠는
JavaScript에서 생성하지 않고 HTML에 직접 작성한다.

단, 사용자 동작이나 비동기 데이터에 따라 실행 중에 생성되어야 하는 UI처럼
JavaScript 생성이 기능적으로 필요한 경우에는 예외로 한다.

예:
- API 또는 서버 데이터로 동적으로 생성되는 목록
- 검색 결과
- 사용자 입력에 따라 추가되는 항목
- 알림, 토스트 등 일시적으로 생성되는 UI
- 기능상 런타임 생성이 필요한 컴포넌트

이 경우에도 기존 HTML 구조나 컴포넌트를 재사용할 수 있는지 먼저 확인하고,
JavaScript에서 불필요하게 대규모 HTML 구조를 생성하지 않는다.

다음 방식을 피한다.

- 기존 함수를 복사한 유사 함수 생성
- 동일 요소에 불필요한 이벤트 중복 등록
- 전역 변수 남용
- HTML에 inline JavaScript 작성
- 단순한 CSS 문제를 JavaScript로 해결
- 임시방편 코드 누적

함수는 하나의 명확한 책임을 갖도록 작성한다.

---

## 10. Refactoring

작업이 끝난 후 새로운 코드를 계속 덧붙이는 방식으로 마무리하지 않는다.

현재 작업 범위에서 다음 문제가 발생했다면 함께 정리한다.

- 중복 CSS
- 불필요한 override
- 사용하지 않는 선언
- 중복 selector
- 중복 함수
- 불필요한 event listener
- 동일한 값을 반복하는 코드

단, 현재 요청과 관련 없는 프로젝트 전체를 임의로 리팩터링하지 않는다.

---

## 11. 작업 완료 후 검증

코드를 수정한 후 반드시 다음 항목을 확인한다.

1. 요청한 디자인 또는 기능이 정상적으로 구현되었는가
2. 기존 스타일과 충돌하지 않는가
3. 불필요한 CSS override가 추가되지 않았는가
4. 기존 breakpoint 정책을 지켰는가
5. Desktop / Tablet / Mobile에서 문제가 없는가
6. 기존 기능이 의도치 않게 변경되지 않았는가
7. 중복 CSS 또는 JavaScript가 발생하지 않았는가
8. 임시방편 코드가 추가되지 않았는가
9. 기존 디자인 토큰을 재사용할 수 있었는데 하드코딩한 값이 없는가
10. 사용하지 않는 코드가 새롭게 발생하지 않았는가

문제가 발견되면 새로운 코드를 추가해서 덮어쓰기보다 기존 코드의 원인을 찾아 수정하는 방식을 우선한다.

---

## 12. 핵심 원칙

이 프로젝트에서 가장 중요한 원칙은 다음과 같다.

**새 코드를 추가해서 기존 문제를 덮어쓰기 전에 기존 구조에서 올바르게 해결할 수 있는지 먼저 확인한다.**

**디자인 토큰과 기존 컴포넌트를 최대한 재사용한다.**

**작은 수정 때문에 전체 프로젝트 구조를 불필요하게 변경하지 않는다.**

**단기적으로 동작하는 코드보다 장기적으로 읽고 수정하기 쉬운 코드를 작성한다.**